# Admin Portal Backend Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# JWT Secret for Admin Authentication (REQUIRED - Generate a strong random secret)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# API Keys for Service Communication (Generated Keys)
DOCTOR_SERVICE_API_KEY=doctor_api_key_generate_unique_string_here
PATIENT_SERVICE_API_KEY=patient_api_key_generate_unique_string_here

# External Service URLs
DOCTOR_SERVICE_URL=http://localhost:3000/api
PATIENT_SERVICE_URL=http://localhost:3000/api

# Admin Database Connection (Optional - can use same as main DB)
DATABASE_URL="postgresql://username:password@localhost:5432/healthcare_portal?schema=public"

# Allowed Origins for CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5174

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@healthcare.com
APP_NAME=Healthcare Admin Portal

# Session Configuration
SESSION_TIMEOUT=30m
TOKEN_EXPIRY=8h
